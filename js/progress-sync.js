(function(){
  'use strict';
  const R=window.StepProgressSync,C=R.config,U=R.util,S=R.storage,A=R.auth,B=R.backupModel,CL=R.classifier,ST=CL.STATES;
  let running=null,checkpointTimer=null,checkpointFirstAt=0,cloudState={manifest:null,file:null,files:[],migrated:false};
  const inFlightKeys=new Set(),dirtyAgainKeys=new Set(),followupKeys=new Set();
  let decisionExpected={};
  let uiState={status:'Off',detail:'Progress is stored locally on this device.',lastBackup:'',lastError:'',account:'',summary:null,transfer:null};
  const getMap=async k=>await R.meta.get(k,{})||{};
  const setMap=(k,v)=>R.meta.set(k,v||{});
  const emit=()=>{try{window.dispatchEvent(new CustomEvent('stepsync:state',{detail:{...uiState}}));}catch(e){}try{renderUi();renderProgressTransfer();}catch(e){console.warn('Sync UI render failed',e);}};
  function setStatus(status,detail='',extra={}){uiState={...uiState,status,detail,...extra};emit();}
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  function fmtTime(v){if(!v)return '—';try{return new Date(v).toLocaleString();}catch(e){return String(v);}}
  function fmtBytes(n){n=Math.max(0,Number(n)||0);const u=['B','KB','MB','GB'];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++;}return `${n.toFixed(i?2:0)} ${u[i]}`;}
  function setTransfer(patch){uiState.transfer=patch?{...(uiState.transfer||{}),...patch}:null;renderProgressTransfer();}
  function ensureTransferUi(){const host=document.getElementById('stepSyncDecisionNotice');if(!host||document.getElementById('stepProgressTransfer'))return;if(!document.getElementById('stepTransferStyle')){const s=document.createElement('style');s.id='stepTransferStyle';s.textContent='.sync-progress-transfer{margin:12px 0;padding:12px;border:1px solid var(--sync-line);border-radius:13px;background:var(--sync-soft);font-size:11px;color:var(--sync-muted)}.sync-transfer-head{display:flex;justify-content:space-between;gap:10px;color:var(--sync-ink);margin-bottom:7px}.sync-transfer-track{height:9px;background:#dce8ee;border-radius:999px;overflow:hidden;margin-bottom:7px}.sync-transfer-track i{display:block;height:100%;width:0;background:linear-gradient(90deg,#0ea5e9,#22c55e);transition:width .2s}.sync-progress-transfer>div+div{margin-top:4px}';document.head.appendChild(s);}const box=document.createElement('div');box.id='stepProgressTransfer';box.className='sync-progress-transfer';box.innerHTML='<div class="sync-transfer-head"><b id="stepTransferTitle">Progress transfer</b><span id="stepTransferPct">0%</span></div><div class="sync-transfer-track"><i id="stepTransferBar"></i></div><div id="stepTransferForm"></div><div id="stepTransferBytes"></div>';host.parentNode.insertBefore(box,host);}
  function renderProgressTransfer(){ensureTransferUi();const box=document.getElementById('stepProgressTransfer'),t=uiState.transfer;if(!box)return;box.hidden=!t;if(!t)return;const total=Math.max(0,Number(t.totalBytes)||0),done=Math.min(total,Math.max(0,Number(t.doneBytes)||0)),pct=total?Math.round(done/total*100):0;document.getElementById('stepTransferTitle').textContent=t.direction==='download'?'Downloading Progress':'Uploading Progress';document.getElementById('stepTransferPct').textContent=pct+'%';document.getElementById('stepTransferBar').style.width=pct+'%';document.getElementById('stepTransferForm').textContent=`${t.form||'Preparing…'} • ${Math.min(t.index||0,t.count||0)} of ${t.count||0} forms`;document.getElementById('stepTransferBytes').textContent=`${fmtBytes(done)} / ${fmtBytes(total)}${t.currentBytes?` • current ${fmtBytes(t.currentBytes)}`:''}`;}
  function escHtml(v){return String(v==null?'':v).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));}
  function escapeQ(s){return String(s).replace(/\\/g,'\\\\').replace(/'/g,"\\'");}
  function localDateKey(d=new Date()){const z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}`;}
  function entityLabel(e,key=''){return e?.kind==='qbank'||key===B.qbankKey?'Qbank':String(e?.formId||key||'Progress');}

  function emptyManifest(deviceId=''){return {type:C.MANIFEST_TYPE,schemaVersion:2,appId:C.CLOUD.appId,updatedAt:U.iso(),build:C.BUILD,lastWriterDeviceId:deviceId,forms:{},qbank:null};}
  function cloneManifest(m){return U.clone(m||emptyManifest());}
  function allEntries(m){const out={};for(const [k,e] of Object.entries(m?.forms||{}))if(e)out[k]={...e,key:k,kind:'form'};if(m?.qbank)out[B.qbankKey]={...m.qbank,key:B.qbankKey,kind:'qbank'};return out;}
  function setEntry(m,key,e){if(key===B.qbankKey)m.qbank=e?{...e,key:undefined}:null;else{m.forms=m.forms||{};if(e)m.forms[key]={...e,key:undefined};else delete m.forms[key];}}
  function normalizeV2Entry(key,e,kind){const formUid=e.formUid||e.formId||(key.includes('@@')?key.split('@@')[0]:key);return {key:B.entityKey(formUid),kind:kind||e.kind||(key===B.qbankKey?'qbank':'form'),formId:e.formId||'',formUid,bankHash:e.bankHash||e.formRevision?.bankHash||'',qidSchemaVersion:Number(e.qidSchemaVersion||e.formRevision?.qidSchemaVersion||0),questionCount:Number(e.questionCount||e.formRevision?.questionCount||0),currentVersionId:String(e.progressRevisionId||e.currentVersionId||e.backupId||''),progressRevisionId:String(e.progressRevisionId||e.currentVersionId||e.backupId||''),parentProgressRevisionId:e.parentProgressRevisionId||e.parentRevisionId||e.previousVersionId||null,previousVersionId:e.previousVersionId||null,driveFileId:e.driveFileId||e.fileId||null,previousDriveFileId:e.previousDriveFileId||null,checksum:e.checksum||e.contentHash||'',sizeBytes:Number(e.sizeBytes??e.size??0)||0,updatedAt:e.updatedAt||'',deviceId:e.writerDeviceId||e.deviceId||'',writerDeviceId:e.writerDeviceId||e.deviceId||'',deleted:!!(e.deleted||e.deletedAt),previousChecksum:e.previousChecksum||'',previousSizeBytes:Number(e.previousSizeBytes||0)||0,previousUpdatedAt:e.previousUpdatedAt||'',previousDeviceId:e.previousDeviceId||'',previousDeleted:!!e.previousDeleted};}
  function migrateManifest(raw){
    if(!raw||typeof raw!=='object')throw new Error('Cloud manifest is invalid.');
    if(raw.appId&&raw.appId!==C.CLOUD.appId)throw new Error('Cloud manifest belongs to a different application.');
    if(Number(raw.schemaVersion)===2){const m=emptyManifest();Object.assign(m,raw);m.schemaVersion=2;m.forms={};let migrated=false;for(const [k,e] of Object.entries(raw.forms||{})){const n=normalizeV2Entry(k,e,'form'),nk=n.key;if(nk!==k)migrated=true;if(m.forms[nk]){m.forms[nk]=U.validDate(m.forms[nk].updatedAt)>=U.validDate(n.updatedAt)?m.forms[nk]:n;migrated=true;}else m.forms[nk]=n;}m.qbank=raw.qbank?normalizeV2Entry(B.qbankKey,raw.qbank,'qbank'):null;return {manifest:m,migrated};}
    if(Number(raw.schemaVersion)===1||raw.entries){const m=emptyManifest();m.updatedAt=raw.updatedAt||U.iso();m.lastWriterDeviceId=raw.lastWriterDeviceId||'';for(const [k,e] of Object.entries(raw.entries||{})){const n=normalizeV2Entry(k,e,e.kind);if(!n.currentVersionId)n.currentVersionId=String(e.backupId||U.uuid());setEntry(m,n.key,n);}return {manifest:m,migrated:true};}
    throw new Error(`Unsupported cloud manifest schema ${String(raw.schemaVersion)}.`);
  }
  function validateManifest(m){if(!m||m.type!==C.MANIFEST_TYPE||Number(m.schemaVersion)!==2)throw new Error('Cloud manifest is invalid.');if(m.appId&&m.appId!==C.CLOUD.appId)throw new Error('Cloud manifest belongs to a different application.');if(!m.forms||typeof m.forms!=='object'||Array.isArray(m.forms))m.forms={};return m;}
  async function listByName(name){const q=`name = '${escapeQ(name)}' and trashed = false`,params=new URLSearchParams({spaces:'appDataFolder',q,fields:'files(id,name,modifiedTime,size)',orderBy:'modifiedTime desc',pageSize:'20'});const r=await A.driveFetch('https://www.googleapis.com/drive/v3/files?'+params.toString());const d=await r.json();return Array.isArray(d.files)?d.files:[];}
  async function downloadJson(id,label){const r=await A.driveFetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}?alt=media`);return U.parseJson(await r.text(),label);}
  async function downloadJsonMeasured(id,label){const r=await A.driveFetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}?alt=media`),text=await r.text();return {value:U.parseJson(text,label),bytes:new Blob([text]).size};}
  async function validateDownloadedBackup(measured,entry,label){
    const expectedSize=Number(entry?.sizeBytes||0),expectedHash=String(entry?.checksum||''),obj=S.validateBackup(measured?.value,entry);
    if(expectedSize&&Number(measured?.bytes)!==expectedSize)throw new Error(`${label} cloud backup size does not match the committed manifest.`);
    if(expectedHash&&String(obj?.contentHash||'')!==expectedHash)throw new Error(`${label} cloud backup checksum does not match the committed manifest.`);
    const actualHash=await U.sha256Text(U.stable(obj?.payload||{}));
    if(expectedHash&&actualHash!==expectedHash)throw new Error(`${label} cloud backup payload checksum is invalid. Local progress was preserved.`);
    return obj;
  }
  async function createFile(name,mimeType='application/json'){const r=await A.driveFetch('https://www.googleapis.com/drive/v3/files?fields=id,name,modifiedTime,size',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,parents:['appDataFolder'],mimeType})});return await r.json();}
  async function uploadJson(id,obj){const r=await A.driveFetch(`https://www.googleapis.com/upload/drive/v3/files/${encodeURIComponent(id)}?uploadType=media&fields=id,name,modifiedTime,size`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(obj)});return await r.json();}
  async function deleteDriveFile(id){if(!id)return;await A.driveFetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}`,{method:'DELETE'});}
  async function copyDriveFile(id,name){const r=await A.driveFetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}/copy?fields=id,name,modifiedTime,size`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,parents:['appDataFolder']})});return await r.json();}
  async function removeDuplicateFiles(files,keep){for(const f of files||[]){if(f.id===keep)continue;try{await deleteDriveFile(f.id);}catch(e){console.warn('Could not remove duplicate manifest',e);}}}
  async function readManifest(){const files=await listByName(C.MANIFEST_FILE);if(!files.length){cloudState={manifest:null,file:null,files:[],migrated:false};return cloudState;}let lastErr=null;for(const file of files){try{const raw=await downloadJson(file.id,'Google Drive progress manifest'),mg=migrateManifest(raw);cloudState={manifest:validateManifest(mg.manifest),file,files,migrated:mg.migrated};return cloudState;}catch(e){lastErr=e;console.warn('Ignoring invalid progress manifest candidate',e);}}throw lastErr||new Error('No valid Google Drive progress manifest was found.');}
  async function writeManifest(manifest){manifest=validateManifest(manifest);manifest.updatedAt=U.iso();manifest.build=C.BUILD;manifest.appId=C.CLOUD.appId;manifest.lastWriterDeviceId=await R.meta.deviceId();let file=cloudState.file,created=false;if(!file){file=await createFile(C.MANIFEST_FILE);created=true;}try{const up=await uploadJson(file.id,manifest);await removeDuplicateFiles(cloudState.files||[],file.id);cloudState={manifest,file:{...file,...up},files:[{...file,...up}],migrated:false};return up;}catch(e){if(created)try{await deleteDriveFile(file.id);}catch(_e){}throw e;}}
  function versionFileName(entity,versionId,prefix='form'){const short=String(versionId).replace(/-/g,'').slice(0,12);if(entity.kind==='qbank')return `${C.CLOUD.driveFilePrefix}.qbank.${short}.json`;return `${C.CLOUD.driveFilePrefix}.${prefix}.${U.safeName(entity.formUid||entity.formId)}.${short}.json`;}

  async function loadV2Meta(local,manifest){
    const meta=await getMap('formSyncMetaV2'),entries=allEntries(manifest),legacyDone=!!(await R.meta.get('v2LegacyMetaMigrated',false));
    const legacyKnown=legacyDone?{}:await getMap('knownCloud'),legacyHashes=legacyDone?{}:await getMap('lastBackedUpHash'),legacyDirty=legacyDone?{}:await getMap('dirtyKeys'),legacyDirtyAll=legacyDone?null:await R.meta.get('dirtyAll',null),legacyTombs=legacyDone?{}:await getMap('deleteTombstones');let changed=false;
    for(const oldKey of Object.keys(meta)){if(!oldKey.includes('@@'))continue;const [id,hash]=oldKey.split('@@'),match=Object.values(local.index||{}).find(e=>e.kind==='form'&&String(e.formId)===id&&(!hash||hash==='NOHASH'||String(e.bankHash)===hash));if(match&&!meta[match.key])meta[match.key]=meta[oldKey];if(match){delete meta[oldKey];changed=true;}}
    const keys=new Set([...Object.keys(local.index||{}),...Object.keys(entries),...Object.keys(meta),...Object.keys(legacyTombs)]);
    for(const key of keys){
      const l=local.index?.[key],c=entries[key];
      if(!meta[key]){
        const knownId=legacyKnown[key]?.backupId||'',base=c&&knownId&&knownId===c.currentVersionId?c.currentVersionId:(c&&l&&l.contentHash&&l.contentHash===c.checksum?c.currentVersionId:'');
        meta[key]={baseCloudVersionId:base,lastKnownCloudVersionId:c?.currentVersionId||'',localContentHash:l?.contentHash||legacyHashes[key]||'',updatedAt:l?.modifiedAt||U.iso(),dirty:!!(legacyDirtyAll||legacyDirty[key]),deleted:!!legacyTombs[key]};changed=true;
      }else{
        const m=meta[key];if((m.lastKnownCloudVersionId||'')!==(c?.currentVersionId||'')){m.lastKnownCloudVersionId=c?.currentVersionId||'';changed=true;}
        if(l?.contentHash&&m.localContentHash!==l.contentHash&&!m.dirty){m.localContentHash=l.contentHash;m.dirty=!!m.baseCloudVersionId;changed=true;}
      }
    }
    if(changed)await setMap('formSyncMetaV2',meta);
    if(!legacyDone){await R.meta.set('v2LegacyMetaMigrated',true);for(const k of['knownCloud','lastBackedUpHash','dirtyKeys','dirtyAll','deleteTombstones','protectedDeletes'])try{await R.meta.del(k);}catch(_e){}}
    return meta;
  }
  async function saveEntityMeta(key,patch){const map=await getMap('formSyncMetaV2'),cur=map[key]||{baseCloudVersionId:'',lastKnownCloudVersionId:'',localContentHash:'',updatedAt:U.iso(),dirty:false,deleted:false};map[key]={...cur,...patch};await setMap('formSyncMetaV2',map);return map[key];}
  async function markDirtyKey(key,reason){if(window.__STEP_SYNC_APPLYING_REMOTE||window.__STEP_SYNC_IMPORTING_PROGRESS)return;if(inFlightKeys.has(key))dirtyAgainKeys.add(key);await saveEntityMeta(key,{dirty:true,dirtyAgain:inFlightKeys.has(key),deleted:false,updatedAt:U.iso()});setStatus(navigator.onLine===false?'Offline':'Local Changes',reason||'Progress changed locally.');}
  async function keyForProgressFilename(name,detail={}){if(/^QBANK_MODE_progress\.json$/i.test(name))return B.qbankKey;const id=detail?.formId||String(name||'').match(/^(.+?)_(?:progress_save|suspended_test)\.json$/i)?.[1];if(!id)return'';try{const cat=await window.StepExamSyncBridge.catalog(),rec=(cat.forms||[]).find(x=>String(x.id)===String(id));return rec?B.entityKey(rec.formUid||rec.id):'';}catch(e){return B.entityKey(id);}}
  const deletionIntents=new Map();
  function noteDeletionIntent(key){if(key)deletionIntents.set(key,Date.now()+8000);}
  async function settleDeletionIntent(key,reason){await sleep(80);const local=await S.localIndex({flush:false,yieldBetween:true}),exists=!!local.index[key];const intentional=(deletionIntents.get(key)||0)>Date.now();deletionIntents.delete(key);await saveEntityMeta(key,{dirty:true,deleted:!!(intentional&&!exists),localContentHash:local.index[key]?.contentHash||'',updatedAt:U.iso()});setStatus(navigator.onLine===false?'Offline':'Local Changes',reason||'Progress changed locally.');}
  async function recordProgressMutation(detail){if(window.__STEP_SYNC_APPLYING_REMOTE||window.__STEP_SYNC_IMPORTING_PROGRESS)return;const key=await keyForProgressFilename(detail?.filename,detail);if(!key)return;const op=String(detail?.operation||'changed');if(op==='delete')await settleDeletionIntent(key,`${detail?.filename||'progress'} deleted`);else await markDirtyKey(key,`${detail?.filename||'progress'} changed`);const enabled=await R.meta.get('syncEnabled',false);if(!enabled)return;scheduleCheckpoint(op==='delete'?'Explicit progress change':'Durable progress changed',op==='delete'?0:2000);}
  function scheduleCheckpoint(reason,delay=2000){const now=Date.now();if(!checkpointFirstAt)checkpointFirstAt=now;const remaining=Math.max(0,5000-(now-checkpointFirstAt)),wait=Math.min(Math.max(0,delay),remaining);clearTimeout(checkpointTimer);checkpointTimer=setTimeout(()=>{checkpointFirstAt=0;backupNow({reason,interactive:false}).catch(()=>{});},wait);}
  async function qidBaselineBlocked(){const pending=await R.meta.get('qidMigrationPendingBaseline',null);if(!pending)return false;setStatus('Baseline Required','QID migration is complete locally. Use Establish New Cloud Baseline in Settings before normal cloud progress sync resumes.');return true;}

  async function analyze({flush=false,yieldBetween=true}={}){
    const cs=await readManifest(),manifest=cs.manifest||emptyManifest(await R.meta.deviceId()),local=await S.localIndex({flush,yieldBetween}),meta=await loadV2Meta(local,manifest),entries=allEntries(manifest),catalogForms=Array.isArray(local.catalog?.forms)?local.catalog.forms:[],catalogById=new Map(catalogForms.map(r=>[String(r.id),r])),rows=[];let metaChanged=false;
    const keys=new Set([...Object.keys(local.index),...Object.keys(entries),...Object.keys(meta)]);
    for(const key of [...keys].sort()){
      const l=local.index[key]||null,c=entries[key]||null,m=meta[key]||{baseCloudVersionId:'',lastKnownCloudVersionId:'',localContentHash:'',updatedAt:l?.modifiedAt||'',dirty:false,deleted:false};
      let bankHashMismatch=false;if(c?.kind==='form'){const loaded=l||catalogById.get(String(c.formId||'')),sameUid=!l||String(l.formUid||l.formId)===String(c.formUid||c.formId),qidCompatible=Number(l?.qidSchemaVersion||loaded?.qidSchemaVersion||0)>=1&&Number(c.qidSchemaVersion||0)>=1&&(!c.questionCount||Number(l?.questionCount||loaded?.totalQuestions||0)===Number(c.questionCount));if(!sameUid||(loaded&&String(loaded.bankHash||'')!==String(c.bankHash||'')&&!qidCompatible))bankHashMismatch=true;}
      if(!l&&!c&&(m.dirty||m.deleted||m.baseCloudVersionId||m.lastKnownCloudVersionId)){m.baseCloudVersionId='';m.lastKnownCloudVersionId='';m.localContentHash='';m.dirty=false;m.deleted=false;meta[key]=m;metaChanged=true;}
      const localDesc={exists:!!l,contentHash:l?.contentHash||m.localContentHash||'',baseCloudVersionId:m.baseCloudVersionId||'',dirty:!!m.dirty,deleted:!!m.deleted,bankHashMismatch};
      const cloudDesc=c?{...c,bankHashMismatch}:null;let state=CL.classifyFormSync(localDesc,cloudDesc);
      /* Exact content equality safely re-adopts cloud authority, including portable imports. */
      if(l&&c&&!c.deleted&&l.contentHash&&c.checksum&&l.contentHash===c.checksum&&!m.deleted){if(m.baseCloudVersionId!==c.currentVersionId||m.dirty){m.baseCloudVersionId=c.currentVersionId;m.lastKnownCloudVersionId=c.currentVersionId;m.localContentHash=l.contentHash;m.dirty=false;m.updatedAt=l.modifiedAt||m.updatedAt;meta[key]=m;metaChanged=true;}state=ST.ALIGNED;}
      if(!l&&c?.deleted&&!m.deleted&&m.baseCloudVersionId!==c.currentVersionId){m.baseCloudVersionId=c.currentVersionId;m.lastKnownCloudVersionId=c.currentVersionId;m.dirty=false;meta[key]=m;metaChanged=true;state=ST.ALIGNED;}
      rows.push({key,local:l,cloud:c,meta:{...m},state,bankHashMismatch});
    }
    if(metaChanged)await setMap('formSyncMetaV2',meta);
    const count=s=>rows.filter(r=>r.state===s).length;
    const summary={rows,localCount:Object.keys(local.index).length,cloudCount:Object.values(entries).filter(e=>!e.deleted).length,aligned:count(ST.ALIGNED),localOnly:count(ST.LOCAL_ONLY),localAheadSafe:count(ST.LOCAL_AHEAD_SAFE),cloudOnly:count(ST.CLOUD_ONLY),cloudAhead:count(ST.CLOUD_AHEAD),diverged:count(ST.DIVERGED),deletedLocalSafe:count(ST.DELETED_LOCAL_SAFE),deleteConflict:count(ST.DELETE_CONFLICT),bankHashMismatch:count(ST.BANK_HASH_MISMATCH),untrackedBoth:count(ST.UNTRACKED_BOTH),manifestUpdatedAt:cs.manifest?.updatedAt||''};
    return {local,manifest,summary,meta,migrated:cs.migrated};
  }
  function conflictRows(a){return a.summary.rows.filter(r=>[ST.DIVERGED,ST.DELETE_CONFLICT,ST.UNTRACKED_BOTH].includes(r.state));}
  function cloudRows(a){return a.summary.rows.filter(r=>[ST.CLOUD_ONLY,ST.CLOUD_AHEAD].includes(r.state));}
  function localSafeRows(a){return a.summary.rows.filter(r=>[ST.LOCAL_ONLY,ST.LOCAL_AHEAD_SAFE,ST.DELETED_LOCAL_SAFE].includes(r.state));}
  function rememberExpected(rows){decisionExpected={};for(const r of rows||[])decisionExpected[r.key]=r.cloud?.currentVersionId||'';}
  function setAnalysisStatus(a){uiState.summary=a.summary;const conflicts=conflictRows(a),cloud=cloudRows(a),local=localSafeRows(a);rememberExpected([...conflicts,...cloud]);if(conflicts.length)setStatus('Conflict',`${conflicts.length} progress item${conflicts.length===1?' has':'s have'} changes on both sides or no trusted lineage. Nothing was overwritten.`,{summary:a.summary});else if(cloud.length)setStatus('Cloud Newer',`${cloud.length} Google progress item${cloud.length===1?' is':'s are'} available for restore.`,{summary:a.summary});else if(local.length)setStatus('Local Changes',`${local.length} local progress item${local.length===1?' is':'s are'} waiting for a safe checkpoint backup.`,{summary:a.summary});else setStatus('Synced','Local progress and the active Google progress manifest are aligned.',{summary:a.summary});}
  async function persistV2ManifestMigrationIfNeeded(a){
    if(!a?.migrated)return false;const fresh=await readManifest();if(!fresh.migrated)return false;await writeManifest(fresh.manifest);return true;
  }
  async function ensureCloudSession({interactive=false}={}){await A.initialize();if(A.getState().authorized)return true;if(interactive){setStatus('Checking','Redirecting to Google authorization through the shared authentication Worker…');await A.connect();return false;}setStatus('Reconnect','The persistent Worker session is unavailable on this device. Local progress is safe.');return false;}
  async function checkCloud({interactive=false,flush=false,autoApply=true}={}){if(await qidBaselineBlocked())return null;if(navigator.onLine===false){setStatus('Offline','Cloud manifest cannot be checked while offline. Local progress is safe.');return null;}if(!(await ensureCloudSession({interactive})))return null;setStatus('Checking','Checking the small Google Drive progress manifest…');const acct=await A.validate();uiState.account=acct.emailAddress||acct.email||'';let a=await analyze({flush,yieldBetween:true});await persistV2ManifestMigrationIfNeeded(a);const safe=cloudRows(a),rt=window.StepExamSyncBridge?.runtime?.();if(autoApply&&safe.length&&!conflictRows(a).length&&!rt?.examVisible){rememberExpected(safe);await restoreKeys(safe.map(r=>r.key),{reason:'Automatic recovery before clean cloud update',confirmUser:false,interactive:false,bypassQidBaseline:true});a=await analyze({flush:false,yieldBetween:true});}setAnalysisStatus(a);return a;}

  function previousFields(remote){return remote?{parentProgressRevisionId:remote.currentVersionId||null,previousVersionId:remote.currentVersionId||null,previousDriveFileId:remote.driveFileId||null,previousChecksum:remote.checksum||'',previousSizeBytes:Number(remote.sizeBytes||0)||0,previousUpdatedAt:remote.updatedAt||'',previousDeviceId:remote.deviceId||'',previousDeleted:!!remote.deleted}:{parentProgressRevisionId:null,previousVersionId:null,previousDriveFileId:null,previousChecksum:'',previousSizeBytes:0,previousUpdatedAt:'',previousDeviceId:'',previousDeleted:false};}
  async function stageUpload(entity,remote){const versionId='pr_'+U.uuid().replace(/-/g,''),deviceId=await R.meta.deviceId(),meta={versionId,parentProgressRevisionId:remote?.currentVersionId||null,updatedAt:U.iso(),deviceId,contentHash:entity.contentHash},backup=entity.kind==='qbank'?S.makeQbankBackup(entity,meta):S.makeFormBackup(entity,meta),name=versionFileName(entity,versionId),file=await createFile(name);try{const uploaded=await uploadJson(file.id,backup);const entry={key:entity.key,kind:entity.kind,formId:entity.formId||'',formUid:entity.formUid||entity.formId||'',bankHash:entity.bankHash||'',qidSchemaVersion:Number(entity.qidSchemaVersion||0),questionCount:Number(entity.questionCount||0),formRevision:{bankHash:entity.bankHash||'',qidSchemaVersion:Number(entity.qidSchemaVersion||0),questionCount:Number(entity.questionCount||0)},progressRevisionId:versionId,currentVersionId:versionId,driveFileId:file.id,checksum:entity.contentHash,sizeBytes:Number(uploaded.size||new Blob([JSON.stringify(backup)]).size),updatedAt:uploaded.modifiedTime||meta.updatedAt,writerDeviceId:deviceId,deviceId,deleted:false,...previousFields(remote)};return {entry,createdId:file.id,oldPreviousId:remote?.previousDriveFileId||null};}catch(e){try{await deleteDriveFile(file.id);}catch(_e){}throw e;}}
  async function stageDeletion(key,remote,local){const versionId='pr_'+U.uuid().replace(/-/g,''),deviceId=await R.meta.deviceId();return {entry:{key,kind:remote?.kind||local?.kind||(key===B.qbankKey?'qbank':'form'),formId:remote?.formId||local?.formId||'',formUid:remote?.formUid||local?.formUid||remote?.formId||local?.formId||'',bankHash:remote?.bankHash||local?.bankHash||'',qidSchemaVersion:Number(remote?.qidSchemaVersion||local?.qidSchemaVersion||0),questionCount:Number(remote?.questionCount||local?.questionCount||0),progressRevisionId:versionId,currentVersionId:versionId,driveFileId:null,checksum:'',sizeBytes:0,updatedAt:U.iso(),writerDeviceId:deviceId,deviceId,deleted:true,...previousFields(remote)},createdId:null,oldPreviousId:remote?.previousDriveFileId||null};}
  async function estimatedUploadBytes(entity,remote){if(!entity)return 0;const versionId='pr_'+'0'.repeat(32),meta={versionId,parentProgressRevisionId:remote?.currentVersionId||null,updatedAt:'2000-01-01T00:00:00.000Z',deviceId:await R.meta.deviceId(),contentHash:entity.contentHash},obj=entity.kind==='qbank'?S.makeQbankBackup(entity,meta):S.makeFormBackup(entity,meta);return new Blob([JSON.stringify(obj)]).size;}
  async function commitStaged(baseManifest,staged){
    try{
      /* Re-read the tiny manifest after payload upload so a concurrent device change cannot be overwritten. */
      const freshState=await readManifest(),fresh=freshState.manifest||emptyManifest(await R.meta.deviceId()),before=allEntries(baseManifest),now=allEntries(fresh);
      for(const x of staged){const expected=before[x.key]?.currentVersionId||'',current=now[x.key]?.currentVersionId||'';if(expected!==current)throw new Error(`Google progress for ${entityLabel(now[x.key]||before[x.key],x.key)} changed while this device was uploading. Nothing was committed; check again.`);}
      const draft=cloneManifest(fresh);for(const x of staged)setEntry(draft,x.key,x.entry);await writeManifest(draft);
      for(const x of staged){if(x.oldPreviousId&&x.oldPreviousId!==x.entry.previousDriveFileId)try{await deleteDriveFile(x.oldPreviousId);}catch(e){console.warn('Could not trim old previous cloud recovery',e);}}
      return draft;
    }catch(e){for(const x of staged)if(x.createdId)try{await deleteDriveFile(x.createdId);}catch(_e){}throw e;}
  }
  async function commitLocalMeta(staged,localIndex){const map=await getMap('formSyncMetaV2'),changed=staged.some(x=>dirtyAgainKeys.has(x.key)),latest=changed?await S.localIndex({flush:false,yieldBetween:true}):null;for(const x of staged){const again=dirtyAgainKeys.has(x.key),l=(again?latest?.index?.[x.key]:localIndex?.[x.key])||null;map[x.key]={...(map[x.key]||{}),baseCloudVersionId:x.entry.currentVersionId,lastKnownCloudVersionId:x.entry.currentVersionId,localContentHash:l?.contentHash||'',updatedAt:l?.modifiedAt||U.iso(),dirty:again,dirtyAgain:false,deleted:false};if(again)followupKeys.add(x.key);dirtyAgainKeys.delete(x.key);}await setMap('formSyncMetaV2',map);const when=cloudState.file?.modifiedTime||U.iso();await R.meta.set('lastBackupAt',when);uiState.lastBackup=when;}
  async function createDailySnapshotIfNeeded(){try{const h=await readHistory(),day=localDateKey();if((h.snapshots||[]).some(s=>s.type==='daily'&&s.day===day))return null;return await createProgressSnapshot({type:'daily',label:`Daily ${day}`,silent:true,history:h});}catch(e){console.warn('Daily recovery snapshot failed',e);return null;}}

  async function backupCore({reason='Manual Back Up Now',interactive=false,forceKeys=null,expectedCloud=null}={}){
    const perf={reason,startedAt:U.iso(),stages:{},bytes:0},clock=()=>typeof performance!=='undefined'&&performance.now?performance.now():Date.now(),t0=clock();
    const enabled=await R.meta.get('syncEnabled',false);if(!enabled&&!interactive){setStatus('Off','Google backup is disabled on this device.');return null;}if(navigator.onLine===false){setStatus('Offline','Backup is pending. Local progress is safe.');return null;}if(!(await ensureCloudSession({interactive})))return null;
    const acct=await A.validate();uiState.account=acct.emailAddress||acct.email||'';setStatus('Checking','Checking cloud lineage before upload…');const a=await analyze({flush:true,yieldBetween:true}),forceSet=new Set(forceKeys||[]),rows=[];
    if(forceSet.size){for(const row of a.summary.rows){if(!forceSet.has(row.key))continue;const expected=expectedCloud?.[row.key]??decisionExpected[row.key]??'',current=row.cloud?.currentVersionId||'';if(expected!==current&&!(row.local&&row.cloud&&!row.cloud.deleted&&row.local.contentHash===row.cloud.checksum))throw new Error(`Google progress for ${entityLabel(row.cloud||row.local,row.key)} changed again. Re-check before choosing a side.`);if(row.local||row.meta.deleted)rows.push(row);}}
    else rows.push(...localSafeRows(a));
    if(!rows.length){await persistV2ManifestMigrationIfNeeded(a);setAnalysisStatus(a);return {uploaded:0,deleted:0,states:a.summary};}
    setStatus('Checking',reason);const staged=[];rows.forEach(row=>inFlightKeys.add(row.key));let totalBytes=0;for(const row of rows)if(row.local&&row.state!==ST.DELETED_LOCAL_SAFE&&!row.meta.deleted)totalBytes+=await estimatedUploadBytes(row.local,row.cloud);let doneBytes=0;setTransfer({direction:'upload',form:'Preparing…',index:0,count:rows.length,totalBytes,doneBytes:0,currentBytes:0});
    try{for(let i=0;i<rows.length;i++){const row=rows[i];setTransfer({form:entityLabel(row.local||row.cloud,row.key),index:i+1,currentBytes:0});if(row.state===ST.DELETED_LOCAL_SAFE||row.meta.deleted){staged.push({key:row.key,...await stageDeletion(row.key,row.cloud,row.local)});}else if(row.local){const item={key:row.key,...await stageUpload(row.local,row.cloud)};staged.push(item);doneBytes+=Number(item.entry.sizeBytes||0);if(doneBytes>totalBytes)totalBytes=doneBytes;setTransfer({doneBytes,totalBytes,currentBytes:Number(item.entry.sizeBytes||0)});}await sleep(0);}const tc=clock();await commitStaged(a.manifest,staged);perf.stages.manifestCommitMs=clock()-tc;await commitLocalMeta(staged,a.local.index);}finally{rows.forEach(row=>inFlightKeys.delete(row.key));setTimeout(()=>setTransfer(null),700);}
    await createDailySnapshotIfNeeded();const after=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(after);perf.bytes=staged.reduce((n,x)=>n+Number(x.entry.sizeBytes||0),0);perf.totalMs=clock()-t0;console.debug('[Progress Sync timing]',perf);return {uploaded:staged.filter(x=>!x.entry.deleted).length,deleted:staged.filter(x=>x.entry.deleted).length,summary:after.summary};
  }
  async function backupNow(opts={}){if(await qidBaselineBlocked())return null;if(running)return running;running=backupCore(opts).catch(async e=>{await handleError(e);throw e;}).finally(()=>{running=null;if(dirtyAgainKeys.size||followupKeys.size){followupKeys.clear();scheduleCheckpoint('Progress changed during upload',0);}});return running;}
  async function keepLocal(key){const a=await analyze({flush:false,yieldBetween:true}),row=a.summary.rows.find(r=>r.key===key);if(!row)throw new Error('Progress item no longer exists.');if(!row.local&& !row.meta.deleted)throw new Error('There is no local progress to keep.');return backupNow({reason:`Keep this device: ${entityLabel(row.local||row.cloud,key)}`,interactive:true,forceKeys:[key],expectedCloud:{[key]:(decisionExpected[key] ?? row.cloud?.currentVersionId ?? '')}});}

  async function restoreKeys(keys,{reason='Saved before cloud restore',confirmUser=false,interactive=true,bypassQidBaseline=false}={}){
    if(!bypassQidBaseline&&await qidBaselineBlocked())return null;const rt=window.StepExamSyncBridge?.runtime?.();if(rt?.examVisible)throw new Error('Leave the active exam before restoring cloud progress.');if(!(await ensureCloudSession({interactive})))return null;const wanted=new Set(keys||[]),a=await analyze({flush:true,yieldBetween:true}),rows=a.summary.rows.filter(r=>wanted.has(r.key)&&r.cloud);
    if(!rows.length)throw new Error('The selected cloud progress is no longer available.');for(const row of rows){const exp=decisionExpected[row.key];if(exp!==undefined&&exp!==(row.cloud?.currentVersionId||''))throw new Error(`Google progress for ${entityLabel(row.cloud,row.key)} changed again. Re-check before restoring.`);}
    if(confirmUser&&!confirm(`Restore ${rows.length} Google progress item${rows.length===1?'':'s'} to this device? Existing matching local progress will be saved as a local recovery point first.`))return null;
    const localKeys=rows.filter(r=>r.local).map(r=>r.key);if(localKeys.length)await S.createLocalRecoveries(localKeys,reason);setStatus('Checking','Restoring selected Google progress…');const map=await getMap('formSyncMetaV2'),totalBytes=rows.reduce((n,r)=>n+Number(r.cloud?.sizeBytes||0),0);let doneBytes=0;setTransfer({direction:'download',form:'Preparing…',index:0,count:rows.length,totalBytes,doneBytes:0,currentBytes:0});
    try{for(let i=0;i<rows.length;i++){const row=rows[i],e=row.cloud;setTransfer({form:entityLabel(e,row.key),index:i+1,currentBytes:0});if(e.deleted)await S.applyDeletion(e);else{if(!e.driveFileId)throw new Error(`Cloud file is missing for ${entityLabel(e,row.key)}.`);const label=entityLabel(e,row.key),measured=await downloadJsonMeasured(e.driveFileId,label),obj=await validateDownloadedBackup(measured,e,label);await S.applyBackup(e,obj);doneBytes+=measured.bytes;setTransfer({doneBytes,totalBytes:Math.max(totalBytes,doneBytes),currentBytes:measured.bytes});}const latest=await S.localIndex({flush:false,yieldBetween:false}),l=latest.index[row.key];map[row.key]={...(map[row.key]||{}),baseCloudVersionId:e.currentVersionId,lastKnownCloudVersionId:e.currentVersionId,localContentHash:e.deleted?'':(l?.contentHash||e.checksum),updatedAt:U.iso(),dirty:!!(l&&!e.deleted&&l.contentHash!==e.checksum),deleted:false};await sleep(0);}}finally{setTimeout(()=>setTransfer(null),700);}await setMap('formSyncMetaV2',map);const after=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(after);await renderRecovery();return {restored:rows.length};
  }
  async function restoreFromCloud(){const a=await analyze({flush:false,yieldBetween:true}),rows=cloudRows(a);if(!rows.length){setAnalysisStatus(a);return null;}rememberExpected(rows);return restoreKeys(rows.map(r=>r.key),{confirmUser:true});}
  async function useCloud(key){return restoreKeys([key],{confirmUser:false});}

  async function readHistory(){const files=await listByName(C.HISTORY_MANIFEST_FILE);if(!files.length)return {type:C.HISTORY_MANIFEST_TYPE,schemaVersion:C.HISTORY_SCHEMA_VERSION,appId:C.CLOUD.appId,updatedAt:U.iso(),snapshots:[],_file:null,_files:[]};const file=files[0],raw=await downloadJson(file.id,'Progress history manifest');if(!raw||raw.type!==C.HISTORY_MANIFEST_TYPE)throw new Error('Progress history manifest is invalid.');const schema=Number(raw.schemaVersion||0);if(schema!==1&&schema!==Number(C.HISTORY_SCHEMA_VERSION))throw new Error(`Unsupported progress history schema ${String(raw.schemaVersion)}.`);if(raw.appId&&raw.appId!==C.CLOUD.appId)throw new Error('Progress history belongs to a different application.');const h={...raw,schemaVersion:C.HISTORY_SCHEMA_VERSION,appId:C.CLOUD.appId,snapshots:Array.isArray(raw.snapshots)?raw.snapshots:[],_file:file,_files:files};return h;}
  async function writeHistory(h){const payload={type:C.HISTORY_MANIFEST_TYPE,schemaVersion:C.HISTORY_SCHEMA_VERSION,appId:C.CLOUD.appId,updatedAt:U.iso(),snapshots:h.snapshots||[]};let file=h._file,created=false;if(!file){file=await createFile(C.HISTORY_MANIFEST_FILE);created=true;}try{const up=await uploadJson(file.id,payload);await removeDuplicateFiles(h._files||[],file.id);return {...payload,_file:{...file,...up},_files:[{...file,...up}]};}catch(e){if(created)try{await deleteDriveFile(file.id);}catch(_e){}throw e;}}
  async function deleteSnapshotFiles(snap,{strict=false}={}){const errors=[];for(const e of Object.values(snap?.entities||{}))if(e.snapshotDriveFileId)try{await deleteDriveFile(e.snapshotDriveFileId);}catch(err){errors.push(err);console.warn('Snapshot cleanup failed',err);}if(strict&&errors.length)throw new Error(`Could not remove ${errors.length} obsolete automatic snapshot file(s).`);return errors;}
  async function createProgressSnapshot({type='manual',label='',silent=false,history=null}={}){if(!(await ensureCloudSession({interactive:!silent})))return null;const cs=await readManifest(),manifest=cs.manifest;if(!manifest||!Object.keys(allEntries(manifest)).length){if(!silent)throw new Error('No current Google progress exists to snapshot.');return null;}let h=history||await readHistory();const id=U.uuid(),createdAt=U.iso(),snap={id,type,createdAt,day:type==='daily'?localDateKey(new Date(createdAt)):'',label:label||'',entities:{}};try{for(const [key,e] of Object.entries(allEntries(manifest))){const se={key,kind:e.kind,formId:e.formId||'',formUid:e.formUid||e.formId||'',bankHash:e.bankHash||'',versionId:e.currentVersionId,checksum:e.checksum||'',sizeBytes:e.sizeBytes||0,updatedAt:e.updatedAt||'',deleted:!!e.deleted,snapshotDriveFileId:null};if(!e.deleted&&e.driveFileId){const label=entityLabel(e,key),cp=await copyDriveFile(e.driveFileId,`${C.CLOUD.driveFilePrefix}.snapshot.${type}.${id.slice(0,8)}.${U.safeName(label)}.json`),measured=await downloadJsonMeasured(cp.id,label);await validateDownloadedBackup(measured,e,label);se.snapshotDriveFileId=cp.id;}snap.entities[key]=se;await sleep(0);}}catch(e){await deleteSnapshotFiles(snap);throw e;}const previousDaily=type==='daily'?(h.snapshots||[]).filter(s=>s.type==='daily'):[];h.snapshots.push(snap);
    /* Commit the verified replacement first. Automatic cleanup is awaited; manual snapshots are untouched. */
    h=await writeHistory(h);if(type==='daily'&&previousDaily.length){try{for(const old of previousDaily)await deleteSnapshotFiles(old,{strict:true});const ids=new Set(previousDaily.map(s=>s.id));h.snapshots=h.snapshots.filter(s=>!ids.has(s.id));h=await writeHistory(h);}catch(e){setStatus('Synced','New daily recovery is valid, but obsolete automatic snapshot cleanup needs retry.',{lastError:e.message});}}
    if(!silent)setStatus('Synced',`Created ${type==='daily'?'daily':'manual'} progress backup snapshot.`);await renderHistory(h);return snap;}
  async function createManualSnapshot(){let label='';try{label=prompt('Optional label for this manual progress backup:','')||'';}catch(e){}return createProgressSnapshot({type:'manual',label});}
  async function deleteSnapshot(id){if(!(await ensureCloudSession({interactive:true})))return;let h=await readHistory(),snap=h.snapshots.find(s=>s.id===id);if(!snap)return;if(!confirm(`Delete this ${snap.type} progress backup from history?`))return;await deleteSnapshotFiles(snap);h.snapshots=h.snapshots.filter(s=>s.id!==id);h=await writeHistory(h);await renderHistory(h);}
  async function restoreSnapshot(id){if(!(await ensureCloudSession({interactive:true})))return;const h=await readHistory(),snap=h.snapshots.find(s=>s.id===id);if(!snap)throw new Error('Progress history snapshot was not found.');if(!confirm(`Restore the ${snap.type} backup from ${fmtTime(snap.createdAt)} as the CURRENT Google progress? This does not silently overwrite this device; local differences will be shown afterward.`))return;const cs=await readManifest(),old=cs.manifest||emptyManifest(await R.meta.deviceId()),oldEntries=allEntries(old),draft=emptyManifest(await R.meta.deviceId()),created=[];
    try{const keys=new Set([...Object.keys(oldEntries),...Object.keys(snap.entities||{})]);for(const key of keys){const se=snap.entities?.[key]||null,oe=oldEntries[key]||null;if(se&&!se.deleted){if(!se.snapshotDriveFileId)throw new Error(`Snapshot file is missing for ${entityLabel(se,key)}.`);const versionId=U.uuid(),cp=await copyDriveFile(se.snapshotDriveFileId,versionFileName({kind:se.kind,formId:se.formId,bankHash:se.bankHash},versionId));created.push(cp.id);setEntry(draft,key,{key,kind:se.kind,formId:se.formId||'',bankHash:se.bankHash||'',currentVersionId:versionId,driveFileId:cp.id,checksum:se.checksum||'',sizeBytes:Number(se.sizeBytes||cp.size||0),updatedAt:U.iso(),deviceId:await R.meta.deviceId(),deleted:false,...previousFields(oe)});}else{setEntry(draft,key,{key,kind:oe?.kind||se?.kind||(key===B.qbankKey?'qbank':'form'),formId:oe?.formId||se?.formId||'',bankHash:oe?.bankHash||se?.bankHash||'',currentVersionId:U.uuid(),driveFileId:null,checksum:'',sizeBytes:0,updatedAt:U.iso(),deviceId:await R.meta.deviceId(),deleted:true,...previousFields(oe)});}await sleep(0);}cloudState={...cs};await writeManifest(draft);}catch(e){for(const id of created)try{await deleteDriveFile(id);}catch(_e){}throw e;}for(const e of Object.values(oldEntries))if(e.previousDriveFileId)try{await deleteDriveFile(e.previousDriveFileId);}catch(_e){}const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);await renderRecovery();}

  async function restorePreviousCloud(key){
    if(!(await ensureCloudSession({interactive:true})))return;const cs=await readManifest(),manifest=cs.manifest;if(!manifest)throw new Error('No current cloud progress exists.');const e=allEntries(manifest)[key];
    if(!e?.previousVersionId)throw new Error('No previous cloud recovery exists for this item.');if(!confirm(`Restore the previous Google copy of ${entityLabel(e,key)} as a new current version?`))return;
    const versionId=U.uuid(),draft=cloneManifest(manifest);let cp=null;
    if(!e.previousDeleted){if(!e.previousDriveFileId)throw new Error('The previous cloud recovery file is missing.');cp=await copyDriveFile(e.previousDriveFileId,versionFileName(e,versionId));}
    const next={...e,currentVersionId:versionId,driveFileId:e.previousDeleted?null:cp.id,checksum:e.previousDeleted?'':(e.previousChecksum||''),sizeBytes:e.previousDeleted?0:(e.previousSizeBytes||Number(cp.size||0)),updatedAt:U.iso(),deviceId:await R.meta.deviceId(),deleted:!!e.previousDeleted,previousVersionId:e.currentVersionId||null,previousDriveFileId:e.driveFileId||null,previousChecksum:e.checksum||'',previousSizeBytes:e.sizeBytes||0,previousUpdatedAt:e.updatedAt||'',previousDeviceId:e.deviceId||'',previousDeleted:!!e.deleted};
    setEntry(draft,key,next);try{await writeManifest(draft);}catch(err){if(cp?.id)try{await deleteDriveFile(cp.id);}catch(_e){}throw err;}const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);await renderRecovery();
  }
  async function deletePreviousCloud(key){
    if(!(await ensureCloudSession({interactive:true})))return;const cs=await readManifest(),manifest=cs.manifest;if(!manifest)return;const e=allEntries(manifest)[key];if(!e?.previousVersionId)return;if(!confirm(`Delete the previous cloud recovery for ${entityLabel(e,key)}?`))return;
    const draft=cloneManifest(manifest),next={...e,previousVersionId:null,previousDriveFileId:null,previousChecksum:'',previousSizeBytes:0,previousUpdatedAt:'',previousDeviceId:'',previousDeleted:false};setEntry(draft,key,next);await writeManifest(draft);if(e.previousDriveFileId)try{await deleteDriveFile(e.previousDriveFileId);}catch(_e){}await renderRecovery();
  }
  async function undoLocalRecovery(key){const map=await S.listLocalRecoveries(),rec=map[key];if(!rec)throw new Error('Local recovery point was not found.');if(!confirm(`Restore the local recovery for ${entityLabel(rec,key)}? It will be treated as imported/untracked progress until compared with Google Drive.`))return;await S.applyRecoveryRecord(rec);await saveEntityMeta(key,{baseCloudVersionId:'',lastKnownCloudVersionId:'',localContentHash:rec.contentHash||'',updatedAt:U.iso(),dirty:true,deleted:false});await S.deleteLocalRecovery(key);const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);await renderRecovery();}

  async function clearCloudProgressBackup(){
    if(!(await ensureCloudSession({interactive:true})))return null;if(!confirm('Clear ONLY current Google progress sync? Active manifest, current files, and one-step cloud recoveries will be deleted. Manual/daily history, Full Form Library Backup, and local progress are preserved.'))return null;
    const cs=await readManifest(),ids=new Set();for(const e of Object.values(allEntries(cs.manifest||{}))){if(e.driveFileId)ids.add(e.driveFileId);if(e.previousDriveFileId)ids.add(e.previousDriveFileId);}
    /* Remove the authority manifest first. Any payload deletion failure then leaves only harmless orphan files. */
    for(const f of cs.files||[])try{await deleteDriveFile(f.id);}catch(e){throw new Error(`Could not clear the active progress manifest: ${e.message||e}`);}
    cloudState={manifest:null,file:null,files:[],migrated:false};for(const id of ids)try{await deleteDriveFile(id);}catch(e){console.warn('Could not delete orphaned active progress file',e);}
    const meta=await getMap('formSyncMetaV2');for(const m of Object.values(meta)){m.baseCloudVersionId='';m.lastKnownCloudVersionId='';if(m.localContentHash)m.dirty=true;}await setMap('formSyncMetaV2',meta);await R.meta.del('lastBackupAt');uiState.lastBackup='';const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);return {removed:ids.size+(cs.files?.length||0)};
  }
  async function replaceCloudProgressWithDevice({confirmed=false,recoveryLabel='Automatic recovery before Replace Current Cloud'}={}){const rt=window.StepExamSyncBridge?.runtime?.();if(rt?.examVisible)throw new Error('Leave the active exam before replacing current cloud progress.');if(!(await ensureCloudSession({interactive:true})))return null;if(!confirmed&&!confirm('Replace CURRENT Google progress with this device? A manual cloud recovery snapshot will be created first when current cloud progress exists. Full Form Library Backup and history remain separate.'))return null;const cs=await readManifest(),old=cs.manifest||emptyManifest(await R.meta.deviceId());if(Object.keys(allEntries(old)).length)await createProgressSnapshot({type:'manual',label:recoveryLabel});const local=await S.localIndex({flush:true,yieldBetween:true}),draft=emptyManifest(local.deviceId),staged=[];
    try{for(const entity of Object.values(local.index)){const st=await stageUpload(entity,null);staged.push({key:entity.key,...st});setEntry(draft,entity.key,{...st.entry,previousVersionId:null,previousDriveFileId:null,previousChecksum:'',previousSizeBytes:0,previousUpdatedAt:'',previousDeviceId:'',previousDeleted:false});await sleep(0);}cloudState={...cs};await writeManifest(draft);}catch(e){for(const x of staged)if(x.createdId)try{await deleteDriveFile(x.createdId);}catch(_e){}throw e;}for(const e of Object.values(allEntries(old))){if(e.driveFileId)try{await deleteDriveFile(e.driveFileId);}catch(_e){}if(e.previousDriveFileId)try{await deleteDriveFile(e.previousDriveFileId);}catch(_e){}}
    await commitLocalMeta(Object.entries(allEntries(draft)).map(([key,entry])=>({key,entry})),local.index);const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);return {uploaded:Object.keys(local.index).length};}

  async function establishNewCloudBaseline(){
    const rt=window.StepExamSyncBridge?.runtime?.();if(rt?.examVisible)throw new Error('Leave the active exam before establishing a cloud baseline.');
    if(!confirm('Establish a new QID-aware cloud baseline from this device? This will create recoveries, mirror the Full Form Library, and replace current per-form cloud progress only after verification.'))return null;
    await S.createLocalRecoveries([],'Safety recovery before QID cloud baseline');
    if(!R.libraryBackup?.backupLibrary)throw new Error('Full Library Backup is not ready. Reload the app and try again.');
    const library=await R.libraryBackup.backupLibrary({confirmed:true});if(!library)throw new Error('Full Library Backup did not complete; progress baseline was not replaced.');
    const progress=await replaceCloudProgressWithDevice({confirmed:true,recoveryLabel:'Automatic recovery before QID cloud baseline'}),verified=await analyze({flush:false,yieldBetween:true});
    if(verified.summary.rows.some(r=>r.state!==ST.ALIGNED))throw new Error('The QID progress baseline did not verify as fully aligned.');
    const marker={createdAt:U.iso(),deviceId:await R.meta.deviceId(),libraryBackupId:library.backupId||'',manifestUpdatedAt:verified.manifest.updatedAt||'',qidSchemaVersion:1};await R.meta.set('qidCloudBaseline',marker);await R.meta.del('qidMigrationPendingBaseline');setStatus('Synced','QID-aware Full Library and per-form progress baseline verified.');return {library,progress,marker};
  }

  async function preparePortableProgressImport(keys){const list=Array.from(new Set(keys||[]));if(list.length)await S.createLocalRecoveries(list,'Saved before local progress import');window.__STEP_SYNC_IMPORTING_PROGRESS=true;return true;}
  async function finishPortableProgressImport(keys){window.__STEP_SYNC_IMPORTING_PROGRESS=false;const list=Array.from(new Set(keys||[])),local=await S.localIndex({flush:false,yieldBetween:true}),map=await getMap('formSyncMetaV2');for(const key of list){const l=local.index[key];map[key]={...(map[key]||{}),baseCloudVersionId:'',lastKnownCloudVersionId:'',localContentHash:l?.contentHash||'',updatedAt:l?.modifiedAt||U.iso(),dirty:!!l,deleted:false,importedAt:U.iso()};}await setMap('formSyncMetaV2',map);if(await R.meta.get('syncEnabled',false)){try{const a=await analyze({flush:false,yieldBetween:true});setAnalysisStatus(a);}catch(e){console.warn('Could not classify imported progress against cloud',e);}}return true;}
  function cancelPortableProgressImport(){window.__STEP_SYNC_IMPORTING_PROGRESS=false;}

  async function runCloudDiagnostics(){if(!(await ensureCloudSession({interactive:true})))return null;const a=await analyze({flush:false,yieldBetween:true}),box=document.getElementById('stepSyncDiagnostics');if(box){const rows=a.summary.rows.filter(r=>r.state!==ST.ALIGNED);box.innerHTML=rows.length?rows.map(r=>`<div class="sync-diag-row"><div class="sync-diag-title"><b>${escHtml(entityLabel(r.local||r.cloud,r.key))}</b><span>${escHtml(r.state)}</span></div><div class="sync-diag-grid"><span>base <code>${escHtml(r.meta.baseCloudVersionId||'—')}</code></span><span>cloud <code>${escHtml(r.cloud?.currentVersionId||'—')}</code></span><span>local hash <code>${escHtml((r.local?.contentHash||'—').slice(0,14))}</code></span><span>cloud hash <code>${escHtml((r.cloud?.checksum||'—').slice(0,14))}</code></span></div></div>`).join(''):`<div class="sync-diag-ok"><b>No active discrepancies</b><span>All matching progress entities classify as ALIGNED.</span></div>`;}return a;}
  async function runValidate(){const r=await S.validateLocal(),inv=document.getElementById('stepSyncInventory');if(inv){const items=[['Loaded forms',r.loadedForms],['Progress forms',r.stats.forms],['Attempts',r.stats.attempts],['Answered',r.stats.answered],['Stem highlights',r.stats.stemHighlights],['Explanation highlights',r.stats.expHighlights],['Qbank tests',r.stats.qbankTests],['Backup entities',r.entities],['Est. local payload',(r.estimatedBytes/1024/1024).toFixed(2)+' MB']];inv.innerHTML=items.map(([a,b])=>`<div class="sync-metric"><span>${a}</span><b>${b}</b></div>`).join('');}return r;}
  async function handleError(e){console.error('Progress sync failed',e);await R.meta.set('lastError',{at:U.iso(),message:e.message||String(e),status:e.status||0,reason:e.reason||''});if(e?.status===401){setStatus('Reconnect','The saved Worker session is no longer valid. Local progress is safe.',{lastError:e.message||String(e)});return;}if(e?.name==='WorkerAuthError'&&e?.status===0){setStatus('Offline','Authentication service is unreachable. Local progress remains pending.',{lastError:e.message||String(e)});return;}setStatus('Error',(e.message||String(e))+(e?.status===403?' Google returned 403; the saved Worker session was preserved.':''),{lastError:e.message||String(e)});}
  async function connect(){uiState.detail='Opening Google authorization…';emit();await A.connect();}
  async function disconnect(){const result=await A.disconnect();await R.meta.set('syncEnabled',false);setStatus('Off',result?.warning?`Google backup is off on this device. ${result.warning}`:'Google backup is off on this device. Existing Drive files are untouched.',{account:'',lastError:''});}

  function tone(){if(uiState.status==='Synced')return'good';if(uiState.status==='Checking')return'busy';if(['Local Changes','Cloud Newer','Conflict','Offline','Reconnect'].includes(uiState.status))return'warn';if(uiState.status==='Error')return'bad';return'';}
  function connected(){return A.getState().authorized&&uiState.status!=='Off'&&uiState.status!=='Reconnect';}
  function installStyles(){if(document.getElementById('stepProgressSyncStyle'))return;const st=document.createElement('style');st.id='stepProgressSyncStyle';st.textContent=`
    #progressSyncPanel{--sync-ink:#17384c;--sync-muted:#637888;--sync-soft:#f5f9fb;--sync-line:#dbe6ed}.sync-status-pill{display:inline-flex;gap:8px;align-items:center;border:1px solid var(--sync-line);border-radius:999px;padding:9px 12px;font-weight:900;font-size:12px;background:#fff}.sync-status-pill i,.sync-tab-dot{width:8px;height:8px;border-radius:50%;display:inline-block;background:#94a3b8}.sync-status-pill.good i,.sync-connected .sync-tab-dot{background:#22c55e}.sync-status-pill.busy i{background:#0ea5e9;box-shadow:0 0 0 5px rgba(14,165,233,.12)}.sync-status-pill.warn i,.sync-attention .sync-tab-dot{background:#f59e0b}.sync-status-pill.bad i{background:#ef4444}.sync-layout{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px}.sync-card,.sync-hero{border:1px solid var(--sync-line);border-radius:20px;padding:18px;background:rgba(255,255,255,.86)}.sync-hero{display:flex;justify-content:space-between;gap:18px;align-items:flex-start}.sync-card h3,.sync-hero h2{margin:0 0 6px}.sync-card p,.sync-hero p,.sync-detail{color:var(--sync-muted);line-height:1.45}.sync-actions,.sync-row-actions,.sync-maintenance{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.sync-meta-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px}.sync-meta{padding:10px;border-radius:13px;background:var(--sync-soft)}.sync-meta span{display:block;font-size:10px;text-transform:uppercase;color:#78909e;font-weight:850}.sync-meta b{display:block;margin-top:3px;font-size:12px}.sync-account{padding:10px 12px;border:1px solid var(--sync-line);border-radius:13px;margin:10px 0}.sync-decision-list,.sync-recovery-list,.sync-history-list{display:flex;flex-direction:column;gap:9px;margin-top:10px}.sync-decision-row,.sync-recovery-row,.sync-history-row{border:1px solid var(--sync-line);border-radius:14px;padding:11px;background:#fbfdfe}.sync-decision-row h4,.sync-recovery-row h4,.sync-history-row h4{margin:0 0 4px}.sync-recovery-summary{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}.sync-recovery-summary>div{padding:10px;border:1px solid var(--sync-line);border-radius:12px;background:var(--sync-soft)}.sync-recovery-summary span{display:block;font-size:10px;text-transform:uppercase;color:#78909e;font-weight:850}.sync-recovery-summary b{display:block;margin-top:3px;font-size:12px}.sync-recovery-retention{font-size:11px!important;margin:10px 0 0!important}.sync-compare{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px;font-size:11px}.sync-compare>div{background:var(--sync-soft);border-radius:10px;padding:8px}.sync-compare b{display:block}.sync-inventory{grid-column:1/-1}#stepSyncInventory{margin-top:12px;display:grid;grid-template-columns:repeat(auto-fit,minmax(135px,1fr));gap:8px}.sync-metric{background:var(--sync-soft);border:1px solid var(--sync-line);border-radius:13px;padding:10px}.sync-metric span{display:block;font-size:10px;text-transform:uppercase;color:#78909e;font-weight:850}.sync-metric b{display:block;margin-top:3px;font-size:16px}.sync-danger{border-color:#e0b8b8!important;color:#8c3030!important}.sync-section-title{margin-top:16px;padding-top:14px;border-top:1px solid var(--sync-line)}.sync-history-groups{display:grid;grid-template-columns:1fr 1fr;gap:12px}.sync-empty{font-size:11px;color:var(--sync-muted);padding:8px 0}.sync-diag-row,.sync-diag-ok{border:1px solid var(--sync-line);border-radius:13px;padding:10px;margin-top:8px}.sync-diag-title{display:flex;justify-content:space-between}.sync-diag-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:5px;margin-top:7px;font-size:10px}.sync-footer{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-top:14px;color:var(--sync-muted);font-size:11px}@media(max-width:900px){.sync-layout{grid-template-columns:1fr}.sync-history-groups{grid-template-columns:1fr}.sync-recovery-summary{grid-template-columns:1fr}}@media(max-width:600px){.sync-compare,.sync-meta-grid{grid-template-columns:1fr}
  `;document.head.appendChild(st);}
  function ensureSurface(){
    const side=document.querySelector('.modern-sidebar');if(side&&!document.getElementById('progressSyncTab')){const btn=document.createElement('button');btn.type='button';btn.id='progressSyncTab';btn.className='menu-tab';btn.innerHTML='Google Backup <span class="sync-tab-dot" aria-hidden="true"></span>';const settings=document.getElementById('settingsTab');settings?side.insertBefore(btn,settings):side.appendChild(btn);}
    const main=document.querySelector('.modern-main'),settingsPanel=document.getElementById('settingsPanel');if(main&&!document.getElementById('progressSyncPanel')){const p=document.createElement('section');p.id='progressSyncPanel';p.className='mode-panel';p.innerHTML=`<div class="sync-hero glass-panel"><div><div class="panel-kicker">Local-first progress • Google Drive</div><h2>Google Backup</h2><p>IndexedDB remains authoritative. Safe per-form changes upload automatically, and clean stale devices download automatically; true divergence always stops for a choice.</p></div><div id="stepSyncStatusPill" class="sync-status-pill"><i></i><span id="stepSyncStatusText">Off</span></div></div><div class="sync-layout"><section class="sync-card"><h3>Current Progress Sync</h3><div id="stepSyncAccountArea"></div><div id="stepSyncDetailText" class="sync-detail"></div><div id="stepSyncActions" class="sync-actions"></div><div class="sync-meta-grid"><div class="sync-meta"><span>Last backup</span><b id="stepSyncLastText">Never</b></div><div class="sync-meta"><span>Manifest</span><b>${C.MANIFEST_FILE}</b></div></div><div id="stepSyncDecisionNotice" class="sync-decision-list"></div></section><section class="sync-card"><h3>Recovery</h3><p>One previous cloud version per item, plus one local recovery point before cloud restore/import replacement.</p><div id="stepSyncRecovery"></div></section><section class="sync-card sync-inventory"><h3>Backup History</h3><p>Exactly one rolling automatic daily snapshot is retained. Manual snapshots remain separate and user-controlled.</p><div class="sync-actions"><button class="primary" data-step-sync-action="manual-snapshot">Create Manual Progress Backup</button></div><div id="stepSyncHistory"></div></section><section class="sync-card sync-inventory"><h3>Advanced</h3><div class="sync-actions"><button class="secondary" data-step-sync-action="check">Check Cloud Manifest</button><button class="secondary" data-step-sync-action="diagnose">Diagnose Sync</button><button class="secondary" data-step-sync-action="validate">Validate Local Progress</button></div><div id="stepSyncInventory"><div class="sync-metric"><span>Status</span><b>Not checked</b></div></div><div id="stepSyncDiagnostics"></div><div class="sync-maintenance"><button class="secondary sync-danger" data-step-sync-action="clear-cloud-progress">Clear Current Cloud Progress</button><button class="secondary sync-danger" data-step-sync-action="replace-cloud-progress">Replace Current Cloud with This Device</button></div><div class="sync-footer"><span>Progress sync/history are separate from Full Form Library Backup.</span><span><a href="./privacy.html" target="_blank" rel="noopener">Privacy Policy</a> · ${C.BUILD}</span></div></section></div>`;settingsPanel?.parentNode?settingsPanel.parentNode.insertBefore(p,settingsPanel.nextSibling):main.appendChild(p);}
    return !!(document.getElementById('progressSyncTab')&&document.getElementById('progressSyncPanel'));
  }
  function renderDecisionRows(){const box=document.getElementById('stepSyncDecisionNotice'),rows=uiState.summary?.rows||[];if(!box)return;const show=rows.filter(r=>[ST.DIVERGED,ST.DELETE_CONFLICT,ST.UNTRACKED_BOTH,ST.CLOUD_AHEAD,ST.CLOUD_ONLY].includes(r.state));if(!show.length){box.innerHTML='';return;}box.innerHTML=show.map(r=>{const label=entityLabel(r.local||r.cloud,r.key),title=r.state===ST.CLOUD_ONLY?'Existing Google progress found':r.state===ST.CLOUD_AHEAD?'Newer Google Backup found':r.state===ST.DELETE_CONFLICT?'Delete conflict':'Both copies changed';let actions='';if(r.state===ST.CLOUD_ONLY)actions=`<button class="primary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="cloud">Restore Progress from Cloud</button>`;else if(r.state===ST.CLOUD_AHEAD)actions=`<button class="primary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="cloud">Use Cloud Backup</button><button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="local">Keep This Device Instead</button>`;else if(r.state===ST.DELETE_CONFLICT)actions=`<button class="secondary sync-danger" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="local">Delete Google Progress Too</button><button class="primary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="cloud">Restore Google Progress</button>`;else actions=`<button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="local">Keep This Device → Cloud</button><button class="primary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-choice="cloud">Use Cloud Backup → This Device</button>`;return `<div class="sync-decision-row"><h4>${escHtml(title)} — ${escHtml(label)}</h4><div class="sync-compare"><div><b>This device</b>${fmtTime(r.local?.modifiedAt||r.meta?.updatedAt)}<br>${r.local?.contentHash?escHtml(r.local.contentHash.slice(0,12)):'No local progress'}</div><div><b>Google Backup</b>${fmtTime(r.cloud?.updatedAt)}<br>${r.cloud?.deleted?'Deleted':escHtml((r.cloud?.checksum||'').slice(0,12))}</div></div><div class="sync-row-actions">${actions}</div></div>`;}).join('');}
  async function renderRecovery(){const box=document.getElementById('stepSyncRecovery');if(!box)return;let cloud=[];try{const cs=cloudState.manifest?cloudState:await readManifest();for(const [key,e] of Object.entries(allEntries(cs.manifest||{})))if(e.previousVersionId)cloud.push({key,e});}catch(e){}const locals=await S.listLocalRecoveries();const localVals=Object.values(locals||{}).sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));const cloudBytes=cloud.reduce((n,{e})=>n+Number(e.previousSizeBytes||0),0);const localBytes=localVals.reduce((n,r)=>n+new Blob([JSON.stringify(r.payload||{})]).size,0);const cloudHtml=cloud.length?cloud.map(({key,e})=>`<div class="sync-recovery-row"><h4>Previous Cloud Recovery — ${escHtml(entityLabel(e,key))}</h4><div>${fmtBytes(e.previousSizeBytes||0)} · ${fmtTime(e.previousUpdatedAt)}</div><div class="sync-row-actions"><button class="secondary" data-step-sync-key="${escHtml(key)}" data-step-sync-action="restore-previous-cloud">Restore Previous</button><button class="secondary" data-step-sync-key="${escHtml(key)}" data-step-sync-action="delete-previous-cloud">Delete Recovery</button></div></div>`).join(''):`<div class="sync-empty">No previous cloud recovery.</div>`;const localHtml=localVals.length?localVals.map(r=>`<div class="sync-recovery-row"><h4>Previous Local Recovery — ${escHtml(entityLabel(r,r.key))}</h4><div>${fmtBytes(new Blob([JSON.stringify(r.payload||{})]).size)} · ${escHtml(r.reason||'Saved before cloud restore')} · ${fmtTime(r.createdAt)}</div><div class="sync-row-actions"><button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-action="undo-local-recovery">Undo Last Cloud Restore</button><button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-action="delete-local-recovery">Delete Recovery</button></div></div>`).join(''):`<div class="sync-empty">No local recovery point.</div>`;box.innerHTML=`<div class="sync-recovery-summary"><div><span>Cloud saved</span><b>${cloud.length} item(s) · ${fmtBytes(cloudBytes)}</b></div><div><span>Local saved</span><b>${localVals.length} item(s) · ${fmtBytes(localBytes)}</b></div></div><p class="sync-recovery-retention">Automatic retention: one previous recovery per progress item. A cloud recovery is replaced and its old file deleted after the next successful backup of that item. A local recovery is replaced when a newer recovery is captured for that item; it is otherwise kept until you restore it or delete it.</p><div class="sync-section-title"><b>Previous Cloud Recovery</b></div><div class="sync-recovery-list">${cloudHtml}</div><div class="sync-section-title"><b>Previous Local Recovery</b></div><div class="sync-recovery-list">${localHtml}</div>`;}
  async function renderHistory(existing=null){const box=document.getElementById('stepSyncHistory');if(!box)return;let h=existing;try{h=h||await readHistory();}catch(e){box.innerHTML=`<div class="sync-empty">History unavailable: ${escHtml(e.message)}</div>`;return;}const mk=arr=>arr.length?arr.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).map(s=>`<div class="sync-history-row"><h4>${s.type==='daily'?'Daily Backup':'Manual Backup'}${s.label?` — ${escHtml(s.label)}`:''}</h4><div>${fmtTime(s.createdAt)} · ${Object.keys(s.entities||{}).length} item(s)</div><div class="sync-row-actions"><button class="secondary" data-step-sync-snapshot="${escHtml(s.id)}" data-step-sync-action="restore-snapshot">Restore</button><button class="secondary" data-step-sync-snapshot="${escHtml(s.id)}" data-step-sync-action="delete-snapshot">Delete</button></div></div>`).join(''):`<div class="sync-empty">None yet.</div>`;box.innerHTML=`<div class="sync-history-groups"><div><div class="sync-section-title"><b>Daily backups</b></div><div class="sync-history-list">${mk((h.snapshots||[]).filter(s=>s.type==='daily'))}</div></div><div><div class="sync-section-title"><b>Manual backups</b></div><div class="sync-history-list">${mk((h.snapshots||[]).filter(s=>s.type==='manual'))}</div></div></div>`;}
  function controls(){if(uiState.status==='Checking')return`<button class="primary" disabled>Checking…</button>`;if(uiState.status==='Reconnect')return`<button class="primary" data-step-sync-action="check">Reconnect Google</button><button class="secondary" data-step-sync-action="disconnect">Disconnect</button>`;if(connected())return`<button class="primary" data-step-sync-action="backup">Back Up Now</button><button class="secondary" data-step-sync-action="restore-cloud">Restore from Cloud</button><button class="secondary" data-step-sync-action="disconnect">Disconnect</button>`;return`<button class="primary" data-step-sync-action="connect">Connect Google Account</button>`;}
  function renderUi(){const pill=document.getElementById('stepSyncStatusPill'),st=document.getElementById('stepSyncStatusText'),dt=document.getElementById('stepSyncDetailText'),aa=document.getElementById('stepSyncAccountArea'),acts=document.getElementById('stepSyncActions'),last=document.getElementById('stepSyncLastText'),tab=document.getElementById('progressSyncTab'),inv=document.getElementById('stepSyncInventory');if(pill)pill.className='sync-status-pill '+tone();if(st)st.textContent=uiState.status;if(dt)dt.textContent=uiState.detail||'';if(aa)aa.innerHTML=connected()?`<div class="sync-account"><b>${escHtml(uiState.account||'Google connected')}</b><div>Worker session stored locally; Drive access token remains memory-only.</div></div>`:`<div class="sync-account">Progress is local. Google backup is optional.</div>`;if(acts)acts.innerHTML=controls();if(last)last.textContent=uiState.lastBackup?fmtTime(uiState.lastBackup):'Never';if(tab){tab.classList.toggle('sync-connected',connected());tab.classList.toggle('sync-attention',['Local Changes','Cloud Newer','Conflict','Offline','Reconnect','Error'].includes(uiState.status));}renderDecisionRows();if(inv&&uiState.summary){const s=uiState.summary,items=[['Local',s.localCount],['Cloud',s.cloudCount],['Aligned',s.aligned],['Local safe',s.localOnly+s.localAheadSafe+s.deletedLocalSafe],['Cloud newer',s.cloudOnly+s.cloudAhead],['Conflict',s.diverged+s.deleteConflict+s.untrackedBoth],['Bank mismatch',s.bankHashMismatch]];inv.innerHTML=items.map(([a,b])=>`<div class="sync-metric"><span>${a}</span><b>${b}</b></div>`).join('');}}
  function activate(){ensureSurface();document.querySelectorAll('.mode-panel.active').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.menu-tab.active').forEach(x=>x.classList.remove('active'));document.getElementById('progressSyncPanel')?.classList.add('active');document.getElementById('progressSyncTab')?.classList.add('active');renderRecovery().catch(()=>{});renderHistory().catch(()=>{});if(connected())checkCloud({interactive:false,flush:false}).catch(()=>{});}

  async function action(a,key,snapshot){if(a==='connect')return connect();if(a==='check')return checkCloud({interactive:true});if(a==='diagnose')return runCloudDiagnostics();if(a==='backup')return backupNow({reason:'Manual Back Up Now',interactive:true});if(a==='restore-cloud')return restoreFromCloud();if(a==='manual-snapshot')return createManualSnapshot();if(a==='clear-cloud-progress')return clearCloudProgressBackup();if(a==='replace-cloud-progress')return replaceCloudProgressWithDevice();if(a==='disconnect')return disconnect();if(a==='validate')return runValidate();if(a==='restore-previous-cloud')return restorePreviousCloud(key);if(a==='delete-previous-cloud')return deletePreviousCloud(key);if(a==='undo-local-recovery')return undoLocalRecovery(key);if(a==='delete-local-recovery'){await S.deleteLocalRecovery(key);return renderRecovery();}if(a==='restore-snapshot')return restoreSnapshot(snapshot);if(a==='delete-snapshot')return deleteSnapshot(snapshot);throw new Error('Unknown Google Backup action: '+String(a||''));}
  async function runUiAction(button,fn){
    if(!button||button.dataset.stepSyncBusy==='1')return;
    button.dataset.stepSyncBusy='1';button.setAttribute('aria-busy','true');
    try{await fn();}
    catch(err){
      console.error('Google Backup action failed',err);
      try{await handleError(err);}catch(_e){}
      const msg=err?.message||String(err);setTimeout(()=>{try{alert(msg);}catch(_e){}},0);
    }finally{
      button.dataset.stepSyncBusy='0';button.removeAttribute('aria-busy');button.disabled=false;
      /* The action may have re-rendered the button. Never leave a replacement control disabled. */
      document.querySelectorAll('#progressSyncPanel [data-step-sync-action][data-step-sync-busy="1"]').forEach(x=>{x.dataset.stepSyncBusy='0';x.removeAttribute('aria-busy');x.disabled=false;});
    }
  }
  document.addEventListener('click',e=>{const tab=e.target?.closest?.('#progressSyncTab');if(tab){e.preventDefault();activate();return;}const other=e.target?.closest?.('.menu-tab:not(#progressSyncTab)');if(other)document.getElementById('progressSyncPanel')?.classList.remove('active');const choice=e.target?.closest?.('[data-step-sync-choice]');if(choice){e.preventDefault();const key=choice.dataset.stepSyncKey;runUiAction(choice,()=>choice.dataset.stepSyncChoice==='local'?keepLocal(key):useCloud(key));return;}const b=e.target?.closest?.('[data-step-sync-action]');if(!b)return;e.preventDefault();runUiAction(b,()=>action(b.dataset.stepSyncAction,b.dataset.stepSyncKey,b.dataset.stepSyncSnapshot));},true);
  document.addEventListener('click',e=>{const el=e.target?.closest?.('[data-action="delete-progress"],[data-action="delete"],[data-v9="delete-attempt"],[data-v9="qbank-delete"],#qbankResetUsedBtn');if(!el)return;try{const card=el.closest?.('[data-form-id]'),id=card?.dataset?.formId;if(id){window.StepExamSyncBridge.catalog().then(cat=>{const r=(cat.forms||[]).find(x=>String(x.id)===String(id));if(r)noteDeletionIntent(B.entityKey(r.formUid||r.id));}).catch(()=>{});}else if(el.matches?.('[data-v9="qbank-delete"],#qbankResetUsedBtn'))noteDeletionIntent(B.qbankKey);}catch(_e){}},true);
  window.addEventListener('stepsim:progress-write',e=>recordProgressMutation(e.detail).catch(console.warn));
  window.addEventListener('stepsim:three-digit-score',e=>{if(window.__STEP_SYNC_APPLYING_REMOTE||window.__STEP_SYNC_IMPORTING_PROGRESS)return;const d=e.detail||{};keyForProgressFilename(`${d.formId}_progress_save.json`,d).then(key=>markDirtyKey(key,'3-digit score changed')).then(()=>R.meta.get('syncEnabled',false)).then(on=>{if(on)scheduleCheckpoint('3-digit score changed',2000);}).catch(()=>{});});
  document.addEventListener('click',e=>{const el=e.target?.closest?.('#finishBlock,#endBlockBtn,#backMenu,#reportSaveMenu,#menuNow,[data-v9="delete-attempt"],[data-v9="qbank-delete"]');if(el)R.meta.get('syncEnabled',false).then(on=>{if(on)scheduleCheckpoint(`Major checkpoint: ${el.id||el.dataset?.v9||'navigation'}`,0);});},true);
  window.addEventListener('online',()=>R.meta.get('syncEnabled',false).then(on=>{if(on)scheduleCheckpoint('Network restored',1800);}));
  document.addEventListener('visibilitychange',()=>{if(document.visibilityState!=='visible')return;R.meta.get('syncEnabled',false).then(async on=>{if(!on)return;const m=await getMap('formSyncMetaV2');if(Object.values(m).some(x=>x.dirty))scheduleCheckpoint('App returned to foreground',2500);else setTimeout(()=>checkCloud({interactive:false,flush:false}).catch(()=>{}),1200);});});

  function idle(fn,timeout=2500){if('requestIdleCallback'in window)requestIdleCallback(()=>fn(),{timeout});else setTimeout(fn,Math.min(timeout,1800));}
  async function boot(){installStyles();
    /* Mount the known working sidebar/panel surface first; never block it on service worker/auth/network work. */
    if(!ensureSurface()){for(const delay of[120,350,900,1800])setTimeout(()=>ensureSurface(),delay);}
    if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js?v='+encodeURIComponent(C.BUILD),{scope:'./'}).catch(e=>console.warn('Service worker registration failed',e));
    try{await A.initialize();}catch(e){console.warn('Cloud auth initialization failed',e);}const enabled=await R.meta.get('syncEnabled',false),acct=await R.meta.get('googleAccount',null),last=await R.meta.get('lastBackupAt','');uiState.account=acct?.email||acct?.emailAddress||'';uiState.lastBackup=last||'';
    if(enabled&&A.getState().authorized){setStatus('Checking','Simulator is ready. Cloud manifest check is deferred until the browser is idle.');idle(async()=>{try{const m=await getMap('formSyncMetaV2'),dirty=Object.values(m).some(x=>x.dirty);if(dirty)await backupNow({reason:'Deferred startup checkpoint',interactive:false});else await checkCloud({interactive:false,flush:false});}catch(e){await handleError(e);}},3000);}else if(enabled)setStatus('Reconnect','Google backup is enabled, but this device no longer has a valid Worker session. Local progress is safe.');else setStatus('Off','Progress is stored locally on this device.');
  }

  R.sync={backupNow,checkCloud,restoreFromCloud,keepLocal,useCloud,clearCloudProgressBackup,replaceCloudProgressWithDevice,establishNewCloudBaseline,runCloudDiagnostics,connect,disconnect,markDirtyKey,getState:()=>({...uiState}),runValidate,createManualSnapshot,restoreSnapshot,deleteSnapshot,restorePreviousCloud,deletePreviousCloud,undoLocalRecovery,preparePortableProgressImport,finishPortableProgressImport,cancelPortableProgressImport,classifyFormSync:CL.classifyFormSync,analyze};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
  /* Keep the Recovery card compact: show the two newest entries and collapse older ones. */
  const _renderRecoveryCompact = renderRecovery;
  renderRecovery = async function(){
    const box=document.getElementById('stepSyncRecovery');
    if(!box)return;
    let cloud=[];try{const cs=cloudState.manifest?cloudState:await readManifest();for(const [key,e] of Object.entries(allEntries(cs.manifest||{})))if(e.previousVersionId)cloud.push({key,e});}catch(e){}
    cloud.sort((a,b)=>String(b.e.previousUpdatedAt||'').localeCompare(String(a.e.previousUpdatedAt||'')));
    const locals=await S.listLocalRecoveries();const localVals=Object.values(locals||{}).sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));
    const localSize=r=>new Blob([JSON.stringify(r.payload||{})]).size,cloudBytes=cloud.reduce((n,{e})=>n+Number(e.previousSizeBytes||0),0),localBytes=localVals.reduce((n,r)=>n+localSize(r),0);
    const cloudRow=({key,e})=>`<div class="sync-recovery-row"><h4>Previous Cloud Recovery — ${escHtml(entityLabel(e,key))}</h4><div>${fmtBytes(e.previousSizeBytes||0)} · ${fmtTime(e.previousUpdatedAt)}</div><div class="sync-row-actions"><button class="secondary" data-step-sync-key="${escHtml(key)}" data-step-sync-action="restore-previous-cloud">Restore Previous</button><button class="secondary" data-step-sync-key="${escHtml(key)}" data-step-sync-action="delete-previous-cloud">Delete Recovery</button></div></div>`;
    const localRow=r=>`<div class="sync-recovery-row"><h4>Previous Local Recovery — ${escHtml(entityLabel(r,r.key))}</h4><div>${fmtBytes(localSize(r))} · ${escHtml(r.reason||'Saved before cloud restore')} · ${fmtTime(r.createdAt)}</div><div class="sync-row-actions"><button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-action="undo-local-recovery">Undo Last Cloud Restore</button><button class="secondary" data-step-sync-key="${escHtml(r.key)}" data-step-sync-action="delete-local-recovery">Delete Recovery</button></div></div>`;
    const compact=(items,row,empty,label)=>{if(!items.length)return `<div class="sync-empty">${empty}</div>`;const older=items.slice(2);return `${items.slice(0,2).map(row).join('')}${older.length?`<details class="sync-recovery-older" style="margin-top:4px;padding:9px 11px;border:1px solid var(--sync-line);border-radius:12px;background:#f8fbfd"><summary style="cursor:pointer;color:#31596f;font-size:11px;font-weight:900">Show ${older.length} older ${label}</summary><div class="sync-recovery-list">${older.map(row).join('')}</div></details>`:''}`;};
    box.innerHTML=`<div class="sync-recovery-summary"><div><span>Cloud saved</span><b>${cloud.length} item(s) · ${fmtBytes(cloudBytes)}</b></div><div><span>Local saved</span><b>${localVals.length} item(s) · ${fmtBytes(localBytes)}</b></div></div><p class="sync-recovery-retention">Showing the 2 newest recoveries in each section. Older recoveries are collapsed below.</p><div class="sync-section-title"><b>Previous Cloud Recovery</b></div><div class="sync-recovery-list">${compact(cloud,cloudRow,'No previous cloud recovery.','cloud recoveries')}</div><div class="sync-section-title"><b>Previous Local Recovery</b></div><div class="sync-recovery-list">${compact(localVals,localRow,'No local recovery point.','local recoveries')}</div>`;
  };
})();
